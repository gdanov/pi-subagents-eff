/**
 * AgentProgress / ProgressSummary types — ported from types.ts in Phase 2.
 *
 * AgentProgress.status discriminates over
 *   "pending" | "running" | "completed" | "failed" | "detached"
 * which becomes a Schema.Literal union in Phase 2.
 */
export {};
