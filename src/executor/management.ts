/**
 * Management mode (action: 'list'|'get'|'create'|'update'|'delete').
 *
 * Replaces agent-management.ts + agent-templates.ts + agent-serializer.ts +
 * agent-selection.ts + agent-scope.ts + slash-commands.ts. Operates on the
 * AgentDirectory service for read; writes go through FileSystem.
 *
 * Implementation lands in Phase 8.
 */
export {};
