/**
 * AsyncStatus / AsyncJobState — ported from types.ts in Phase 2.
 *
 * Both decode/encode to JSON for status.json on disk.
 *   AsyncStatus.state: Schema.Literal("queued","running","complete","failed")
 */
export {};
