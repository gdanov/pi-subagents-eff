/**
 * Process-wide constants that the legacy code embedded in types.ts.
 *
 * Filled in during Phase 2: directory names (ASYNC_DIR, RESULTS_DIR),
 * default artifact-cleanup ages, the SLASH_RESULT_TYPE marker, the
 * widget key, and the resolveTempScopeId helper.
 */
export {};
