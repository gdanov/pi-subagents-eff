/**
 * Chain Clarify TUI (interactive preview/edit dialog).
 *
 * Replaces chain-clarify.ts. Stays imperative; entered via
 * `Effect.tryPromise(() => showChainClarifyDialog(initialState))`.
 *
 * Implementation lands in Phase 9.
 */
export {};
