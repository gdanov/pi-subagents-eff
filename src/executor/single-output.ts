/**
 * Single-agent output post-processing.
 *
 * Replaces single-output.ts. Pure-ish helpers that depend on FileSystem
 * (for writing the saved-output file) and Schema (for shaping the saved
 * payload).
 *
 * Implementation lands in Phase 5.
 */
export {};
