/**
 * SingleResult / Details / ModelAttempt — ported from types.ts in Phase 2.
 *
 * Details is the discriminated union over mode in
 *   "single" | "parallel" | "chain" | "management"
 * which becomes a Schema.TaggedUnion in Phase 2.
 */
export {};
